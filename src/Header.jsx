import React from 'react';
import '../src/App.css'; // Import your CSS file

const Header = () => {
    return (
        <header className="header">
            <div className="header-container">
                <h1 className="header-title">Google Keep</h1>
            </div>
        </header>
    );
}

export default Header;
